version: "3"
services: 
    thyroid:
        build: .
        working_dir: /workspace
        tty: true
        volumes: 
            - ./:/workspace:cached
        ports: 
            - "8888:8888"
        runtime: nvidia
        shm_size: '2gb'
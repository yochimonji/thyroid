version: "3"
services: 
    thyroid:
        build: .
        working_dir: /workspace
        tty: true
        volumes: 
            - ./:/workspace:cached
        # ports: 
        #     - "8888:8888"
        deploy:
            resources:
                reservations:
                    devices:
                        - driver: nvidia
                          capabilities: [gpu]
        shm_size: '2gb'